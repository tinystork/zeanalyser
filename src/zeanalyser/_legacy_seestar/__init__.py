"""Historical helpers relocated from the top-level ``seestar`` package.

The top-level ``seestar`` namespace is reserved for ZeSeestarStacker; ZeAnalyser
ships its own private copy of the legacy GUI helpers it still references so the
Tk interface keeps working in standalone mode.
"""
